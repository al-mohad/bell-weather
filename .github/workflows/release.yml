name: release

on:
  push:
    branches: [main]

permissions:
  contents: write
  pull-requests: write
  id-token: write

concurrency:
  group: release
  cancel-in-progress: false

jobs:
  release:
    # Opt-in. Until the @bellwether scope is claimed on npm and trusted publishing is
    # configured, `changeset publish` cannot succeed, and a workflow that fails on every
    # push to main trains everyone to ignore a red X. Set the repository variable
    # RELEASE_ENABLED=true when the scope is ready:
    #   gh variable set RELEASE_ENABLED --body true
    if: vars.RELEASE_ENABLED == 'true'
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          fetch-depth: 0
      - uses: pnpm/action-setup@v6
      - uses: actions/setup-node@v4
        with:
          node-version: '22'
          registry-url: https://registry.npmjs.org
          cache: pnpm
      - run: pnpm install --frozen-lockfile
      - run: pnpm build

      - name: Version or publish
        uses: changesets/action@v2
        with:
          version: pnpm changeset version
          publish: pnpm changeset publish
          commit: 'chore: release'
          title: 'chore: release'
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
          # Trusted publishing: no long-lived npm token.
          NPM_CONFIG_PROVENANCE: 'true'

      - name: Software bill of materials
        run: pnpm licenses list --json > sbom-licenses.json || true
      - uses: actions/upload-artifact@v4
        with:
          name: sbom
          path: sbom-licenses.json
          retention-days: 90
