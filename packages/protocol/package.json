{
  "name": "@bellwether/protocol",
  "version": "0.1.0",
  "description": "Versioned agent<->harness contract for Bellwether. Language-agnostic JSON-RPC 2.0 over NDJSON stdio.",
  "license": "Apache-2.0",
  "type": "module",
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "publishConfig": {
    "main": "./dist/index.js",
    "types": "./dist/index.d.ts"
  },
  "files": [
    "dist",
    "schema"
  ],
  "scripts": {
    "build": "tsup src/index.ts --format esm --dts --clean",
    "schema": "tsx src/emit-schema.ts schema"
  },
  "dependencies": {
    "zod": "^4.1.12"
  }
}
