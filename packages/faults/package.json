{
  "name": "@bellwether/faults",
  "version": "0.1.0",
  "description": "Seeded fault injection. Deterministic per (run seed, task, trial).",
  "license": "Apache-2.0",
  "type": "module",
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "publishConfig": {
    "main": "./dist/index.js",
    "types": "./dist/index.d.ts"
  },
  "files": [
    "dist"
  ],
  "scripts": {
    "build": "tsup src/index.ts --format esm --dts --clean"
  },
  "dependencies": {
    "@bellwether/core": "workspace:*",
    "@bellwether/solari": "workspace:*",
    "@bellwether/surfaces": "workspace:*"
  }
}
