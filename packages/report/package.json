{
  "name": "@bellwether/report",
  "version": "0.1.0",
  "description": "Static HTML report: leaderboard, per-task table and a step-by-step trace scrubber.",
  "license": "Apache-2.0",
  "type": "module",
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "publishConfig": {
    "main": "./dist/index.js",
    "types": "./dist/index.d.ts"
  },
  "files": [
    "dist"
  ],
  "scripts": {
    "build": "tsup src/index.ts --format esm --dts --clean"
  },
  "dependencies": {
    "@bellwether/runner": "workspace:*"
  }
}
