# Only needed for --driver live. Everything in CI runs on --driver sim.
SOLARI_API_KEY=slr_live_xxxxxxxxxxxxxxxxxxxxx

# Only needed for the agents/claude-cua reference agent.
ANTHROPIC_API_KEY=sk-ant-xxxxxxxxxxxxxxxxxxxxx

# Hard ceiling enforced by the runner before every trial.
BELLWETHER_BUDGET_USD=25
