{
  "name": "@bellwether/core",
  "version": "0.1.0",
  "description": "Shared primitives: seeded RNG, structured logging, OpenTelemetry spans, budgets, errors.",
  "license": "Apache-2.0",
  "type": "module",
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "publishConfig": {
    "main": "./dist/index.js",
    "types": "./dist/index.d.ts"
  },
  "files": [
    "dist"
  ],
  "scripts": {
    "build": "tsup src/index.ts --format esm --dts --clean"
  },
  "dependencies": {
    "@opentelemetry/api": "^1.9.0"
  }
}
