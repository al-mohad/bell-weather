{
  "name": "@bellwether/surfaces",
  "version": "0.1.0",
  "description": "Translates protocol actions into driver input, uniformly across browser and desktop surfaces.",
  "license": "Apache-2.0",
  "type": "module",
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "publishConfig": {
    "main": "./dist/index.js",
    "types": "./dist/index.d.ts"
  },
  "files": [
    "dist"
  ],
  "scripts": {
    "build": "tsup src/index.ts --format esm --dts --clean"
  },
  "dependencies": {
    "@bellwether/core": "workspace:*",
    "@bellwether/protocol": "workspace:*",
    "@bellwether/solari": "workspace:*"
  }
}
