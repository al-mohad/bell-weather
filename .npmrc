# Workspace packages are linked from source; nothing here is published implicitly.
link-workspace-packages=true
save-workspace-protocol=rolling
# Deterministic installs in CI.
prefer-frozen-lockfile=true
# The live Solari SDKs are optional peers: absence must not fail an install.
strict-peer-dependencies=false
